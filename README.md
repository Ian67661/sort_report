--- a/d:/Ian/bublle/sort_report/README.md
+++ b/d:/Ian/bublle/sort_report/README.md
@@ -1,1 +1,50 @@
 # sort_report
+
+## 1. 排序法介紹
+本報告分析以下五種排序演算法：
+- **冒泡排序 (Bubble Sort)**
+- **選擇排序 (Selection Sort)**
+- **插入排序 (Insertion Sort)**
+- **合併排序 (Merge Sort)**
+- **快速排序 (Quick Sort)**
+
+## 2. 複雜度分析
+| 演算法 | 時間 (平均) | 時間 (最差) | 空間複雜度 |
+| :--- | :--- | :--- | :--- |
+| Bubble Sort | $O(n^2)$ | (n^2)$ | (1)$ |
+| Selection Sort | (n^2)$ | (n^2)$ | (1)$ |
+| Insertion Sort | (n^2)$ | (n^2)$ | (1)$ |
+| Merge Sort | (n \log n)$ | (n \log n)$ | (n)$ |
+| Quick Sort | (n \log n)$ | (n^2)$ | (\log n)$ |
+
+## 3. 實驗設計
+- **開發語言**：Python
+- **測試環境**：Windows 10 / Python 3.x
+- **數據來源**：使用 `random` 模組產生範圍 0-10000 的隨機整數。
+- **數據規模 (n)**：100, 500, 1000, 2000, 5000。
+
+## 4. 結果呈現與比較
+*(請在此處貼上執行實作程式碼後產生的數據表格)*
+
+### 效能觀察：
+1. 當 $ 較小時，五種排序法的差距不明顯。
+2. 隨著 $ 增加，(n^2)$ 演算法（如 Bubble Sort）的執行時間呈指數成長。
+3. Merge Sort 與 Quick Sort 在大數據量下表現極其穩定且快速。
+
+## 5. 心得與結論
+透過本次實驗，我觀察到理論複雜度與實際執行效率的高度一致性。
+
+### 個人觀察：
+- **實用性**：雖然 Bubble Sort 最容易理解，但在實際應用中幾乎不會使用，因為其效率低落。
+- **穩定性**：在需要保持相同數值原始順序的場景下，Merge Sort 是比 Quick Sort 更好的選擇。
+- **空間換取時間**：Merge Sort 雖然快速，但需要額外的 (n)$ 空間，這在記憶體受限的環境需要列入考量。
+
+總結來說，選擇排序演算法時，不能只看平均時間複雜度，還需考量數據規模、記憶體限制以及是否需要穩定性。

--- a/d:/Ian/bublle/sort_report/README.md
+++ b/d:/Ian/bublle/sort_report/README.md
@@ -1,1 +1,50 @@
 # sort_report
+
+## 1. 排序法介紹
+本報告分析以下五種排序演算法：
+- **冒泡排序 (Bubble Sort)**
+- **選擇排序 (Selection Sort)**
+- **插入排序 (Insertion Sort)**
+- **合併排序 (Merge Sort)**
+- **快速排序 (Quick Sort)**
+
+## 2. 複雜度分析
+| 演算法 | 時間 (平均) | 時間 (最差) | 空間複雜度 |
+| :--- | :--- | :--- | :--- |
+| Bubble Sort | $O(n^2)$ | (n^2)$ | (1)$ |
+| Selection Sort | (n^2)$ | (n^2)$ | (1)$ |
+| Insertion Sort | (n^2)$ | (n^2)$ | (1)$ |
+| Merge Sort | (n \log n)$ | (n \log n)$ | (n)$ |
+| Quick Sort | (n \log n)$ | (n^2)$ | (\log n)$ |
+
+## 3. 實驗設計
+- **開發語言**：Python
+- **測試環境**：Windows 10 / Python 3.x
+- **數據來源**：使用 `random` 模組產生範圍 0-10000 的隨機整數。
+- **數據規模 (n)**：100, 500, 1000, 2000, 5000。
+
+## 4. 結果呈現與比較
+*(請在此處貼上執行實作程式碼後產生的數據表格)*
+
+### 效能觀察：
+1. 當 $ 較小時，五種排序法的差距不明顯。
+2. 隨著 $ 增加，(n^2)$ 演算法（如 Bubble Sort）的執行時間呈指數成長。
+3. Merge Sort 與 Quick Sort 在大數據量下表現極其穩定且快速。
+
+## 5. 心得與結論
+透過本次實驗，我觀察到理論複雜度與實際執行效率的高度一致性。
+
+### 個人觀察：
+- **實用性**：雖然 Bubble Sort 最容易理解，但在實際應用中幾乎不會使用，因為其效率低落。
+- **穩定性**：在需要保持相同數值原始順序的場景下，Merge Sort 是比 Quick Sort 更好的選擇。
+- **空間換取時間**：Merge Sort 雖然快速，但需要額外的 (n)$ 空間，這在記憶體受限的環境需要列入考量。
+
+總結來說，選擇排序演算法時，不能只看平均時間複雜度，還需考量數據規模、記憶體限制以及是否需要穩定性。

--- a/d:/Ian/bublle/sort_report/README.md
+++ b/d:/Ian/bublle/sort_report/README.md
@@ -1,1 +1,50 @@
 # sort_report
+
+## 1. 排序法介紹
+本報告分析以下五種排序演算法：
+- **冒泡排序 (Bubble Sort)**
+- **選擇排序 (Selection Sort)**
+- **插入排序 (Insertion Sort)**
+- **合併排序 (Merge Sort)**
+- **快速排序 (Quick Sort)**
+
+## 2. 複雜度分析
+| 演算法 | 時間 (平均) | 時間 (最差) | 空間複雜度 |
+| :--- | :--- | :--- | :--- |
+| Bubble Sort | $O(n^2)$ | (n^2)$ | (1)$ |
+| Selection Sort | (n^2)$ | (n^2)$ | (1)$ |
+| Insertion Sort | (n^2)$ | (n^2)$ | (1)$ |
+| Merge Sort | (n \log n)$ | (n \log n)$ | (n)$ |
+| Quick Sort | (n \log n)$ | (n^2)$ | (\log n)$ |
+
+## 3. 實驗設計
+- **開發語言**：Python
+- **測試環境**：Windows 10 / Python 3.x
+- **數據來源**：使用 `random` 模組產生範圍 0-10000 的隨機整數。
+- **數據規模 (n)**：100, 500, 1000, 2000, 5000。
+
+## 4. 結果呈現與比較
+*(請在此處貼上執行實作程式碼後產生的數據表格)*
+
+### 效能觀察：
+1. 當 $ 較小時，五種排序法的差距不明顯。
+2. 隨著 $ 增加，(n^2)$ 演算法（如 Bubble Sort）的執行時間呈指數成長。
+3. Merge Sort 與 Quick Sort 在大數據量下表現極其穩定且快速。
+
+## 5. 心得與結論
+透過本次實驗，我觀察到理論複雜度與實際執行效率的高度一致性。
+
+### 個人觀察：
+- **實用性**：雖然 Bubble Sort 最容易理解，但在實際應用中幾乎不會使用，因為其效率低落。
+- **穩定性**：在需要保持相同數值原始順序的場景下，Merge Sort 是比 Quick Sort 更好的選擇。
+- **空間換取時間**：Merge Sort 雖然快速，但需要額外的 (n)$ 空間，這在記憶體受限的環境需要列入考量。
+
+總結來說，選擇排序演算法時，不能只看平均時間複雜度，還需考量數據規模、記憶體限制以及是否需要穩定性。

--- a/d:/Ian/bublle/sort_report/README.md
+++ b/d:/Ian/bublle/sort_report/README.md
@@ -1,1 +1,50 @@
 # sort_report
+
+## 1. 排序法介紹
+本報告分析以下五種排序演算法：
+- **冒泡排序 (Bubble Sort)**
+- **選擇排序 (Selection Sort)**
+- **插入排序 (Insertion Sort)**
+- **合併排序 (Merge Sort)**
+- **快速排序 (Quick Sort)**
+
+## 2. 複雜度分析
+| 演算法 | 時間 (平均) | 時間 (最差) | 空間複雜度 |
+| :--- | :--- | :--- | :--- |
+| Bubble Sort | $O(n^2)$ | (n^2)$ | (1)$ |
+| Selection Sort | (n^2)$ | (n^2)$ | (1)$ |
+| Insertion Sort | (n^2)$ | (n^2)$ | (1)$ |
+| Merge Sort | (n \log n)$ | (n \log n)$ | (n)$ |
+| Quick Sort | (n \log n)$ | (n^2)$ | (\log n)$ |
+
+## 3. 實驗設計
+- **開發語言**：Python
+- **測試環境**：Windows 10 / Python 3.x
+- **數據來源**：使用 `random` 模組產生範圍 0-10000 的隨機整數。
+- **數據規模 (n)**：100, 500, 1000, 2000, 5000。
+
+## 4. 結果呈現與比較
+*(請在此處貼上執行實作程式碼後產生的數據表格)*
+
+### 效能觀察：
+1. 當 $ 較小時，五種排序法的差距不明顯。
+2. 隨著 $ 增加，(n^2)$ 演算法（如 Bubble Sort）的執行時間呈指數成長。
+3. Merge Sort 與 Quick Sort 在大數據量下表現極其穩定且快速。
+
+## 5. 心得與結論
+透過本次實驗，我觀察到理論複雜度與實際執行效率的高度一致性。
+
+### 個人觀察：
+- **實用性**：雖然 Bubble Sort 最容易理解，但在實際應用中幾乎不會使用，因為其效率低落。
+- **穩定性**：在需要保持相同數值原始順序的場景下，Merge Sort 是比 Quick Sort 更好的選擇。
+- **空間換取時間**：Merge Sort 雖然快速，但需要額外的 (n)$ 空間，這在記憶體受限的環境需要列入考量。
+
+總結來說，選擇排序演算法時，不能只看平均時間複雜度，還需考量數據規模、記憶體限制以及是否需要穩定性。

# sort_report

## 1. 排序法介紹
本報告針對以下五種常見的排序演算法進行深入分析：

- **冒泡排序 (Bubble Sort)**：重複走訪序列，比較相鄰元素並交換順序錯誤者，讓較大元素如氣泡般逐步「浮」到序列末端。
- **選擇排序 (Selection Sort)**：每次從未排序部分尋找最小元素，存放到已排序序列的起始位置。
- **插入排序 (Insertion Sort)**：將待排序元素逐一插入到已排序序列的適當位置，類似整理撲克牌。
- **合併排序 (Merge Sort)**：採用分治法 (Divide and Conquer)，將序列遞迴拆解至最小單位後，再進行有序合併。
- **快速排序 (Quick Sort)**：挑選一基準值 (Pivot)，將序列分為比基準小與比基準大的兩部分，再遞迴處理。

## 2. 複雜度分析

| 演算法 | 時間 (平均) | 時間 (最差) | 空間複雜度 | 穩定性 |
| :--- | :--- | :--- | :--- | :--- |
| **Bubble Sort** | $O(n^2)$ | $O(n^2)$ | $O(1)$ | 穩定 |
| **Selection Sort** | $O(n^2)$ | $O(n^2)$ | $O(1)$ | 不穩定 |
| **Insertion Sort** | $O(n^2)$ | $O(n^2)$ | $O(1)$ | 穩定 |
| **Merge Sort** | $O(n \log n)$ | $O(n \log n)$ | $O(n)$ | 穩定 |
| **Quick Sort** | $O(n \log n)$ | $O(n^2)$ | $O(\log n)$ | 不穩定 |

## 3. 實驗設計
為了觀察演算法在實際環境下的表現，設計實驗如下：

- **開發語言**：Python 3.x
- **數據來源**：使用 `random` 模組產生範圍 $0 \sim 10,000$ 的隨機整數。
- **數據規模 ($n$)**：分別測試 $100, 500, 1000, 2000, 5000$。
- **測量指標**：記錄演算法完成排序所需的執行時間（秒）。

## 4. 結果呈現與比較

### 實驗數據記錄表
| 數據規模 ($n$) | Bubble | Selection | Insertion | Merge | Quick |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 100 | - | - | - | - | - |
| 500 | - | - | - | - | - |
| 1000 | - | - | - | - | - |
| 5000 | - | - | - | - | - |
*(註：請根據程式實測結果填入秒數)*

### 效能觀察：
1. **小數據量**：當 $n$ 較小時，五種排序法的執行時間差距並不明顯。
2. **成長趨勢**：隨著 $n$ 增加，$O(n^2)$ 演算法（如 Bubble Sort）的耗時呈指數級成長。
3. **效率分水嶺**：Merge Sort 與 Quick Sort 在處理大數據量時展現極佳的穩定性與速度。

## 5. 心得與結論
透過本次實驗，我觀察到理論時間複雜度與實際執行效率之間有著高度的一致性。

### 個人觀察與分析：
- **實用性判斷**：雖然 Bubble Sort 邏輯最簡單，但在實際應用中效率極低，僅適合教學或數據極小時使用。
- **穩定性的考量**：在需要保持相同數值原始相對順序的場景下，Merge Sort 是比 Quick Sort 更好的選擇。
- **空間與時間的權衡**：Merge Sort 雖然快速且穩定，但其 $O(n)$ 的額外空間需求在記憶體受限的環境中（如嵌入式系統）需謹慎考慮。

**總結**：選擇排序演算法時，必須根據數據規模、記憶體限制以及是否需要穩定性來綜合評估，沒有絕對最好的演算法，只有最適合該情境的工具。