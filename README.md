# sort_report

**[🏠 排序演算法教學入口](https://ian67661.github.io/sort_report/)** 

## 1. 排序法介紹
本報告針對以下五種常見的排序演算法進行深入分析：

- **冒泡排序 (Bubble Sort)**：重複走訪序列，比較相鄰元素並交換順序錯誤者，讓較大元素如氣泡般逐步「浮」到序列末端。
- **選擇排序 (Selection Sort)**：每次從未排序部分尋找最小元素，存放到已排序序列的起始位置。
- **插入排序 (Insertion Sort)**：將待排序元素逐一插入到已排序序列的適當位置，類似整理撲克牌。
- **合併排序 (Merge Sort)**：採用分治法 (Divide and Conquer)，將序列遞迴拆解至最小單位後，再進行有序合併。
- **快速排序 (Quick Sort)**：挑選一基準值 (Pivot)，將序列分為比基準小與比基準大的兩部分，再遞迴處理。

## 2. 複雜度分析

| 演算法 | 時間 (平均) | 時間 (最差) | 空間複雜度 | 穩定性 |
| :--- | :--- | :--- | :--- | :--- |
| **Bubble Sort** | $O(n^2)$ | $O(n^2)$ | $O(1)$ | 穩定 |
| **Selection Sort** | $O(n^2)$ | $O(n^2)$ | $O(1)$ | 不穩定 |
| **Insertion Sort** | $O(n^2)$ | $O(n^2)$ | $O(1)$ | 穩定 |
| **Merge Sort** | $O(n \log n)$ | $O(n \log n)$ | $O(n)$ | 穩定 |
| **Quick Sort** | $O(n \log n)$ | $O(n^2)$ | $O(\log n)$ | 不穩定 |

## 3. 數據模擬
本階段以程式實作上述五種排序法，並在相同硬體環境下針對不同規模的數據量進行實測，比較排序 $n$ 個隨機整數所需的執行時間。

- **實作方式**：撰寫 Python 3.x 自動化測試腳本，針對每種演算法進行獨立實作。
- **數據來源**：透過 `random` 模組產生範圍 $0 \sim 10,000$ 之間的隨機數，確保測試的公正性。
- **測試規模 ($n$)**：分別針對 $n = 100, 500, 1000, 2000, 5000$ 進行數據模擬。
- **測量指標**：記錄演算法完成排序所需的執行時間（秒）。

### 數據模擬實測數據表 (秒)
| 數據規模 ($n$) | Bubble (秒) | Selection (秒) | Insertion (秒) | Merge (秒) | Quick (秒) |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 100 | 0.0005 | 0.0003 | 0.0002 | 0.0001 | 0.0001 |
| 500 | 0.0132 | 0.0085 | 0.0048 | 0.0007 | 0.0005 |
| 1000 | 0.0541 | 0.0342 | 0.0195 | 0.0016 | 0.0011 |
| 2000 | 0.2150 | 0.1380 | 0.0780 | 0.0035 | 0.0023 |
| 5000 | 1.3520 | 0.8750 | 0.4950 | 0.0092 | 0.0061 |

## 4. 結果呈現與比較
本節針對上述實測數據進行效能分析，比較不同排序演算法在實際運行時的效率差異與特性。

### 效能特性分析表
| 比較維度 | 基礎排序 ($O(n^2)$) | 進階排序 ($O(n \log n)$) | 實驗觀察結論 |
| :--- | :--- | :--- | :--- |
| **數據量適應性** | 僅適合小型數據 ($n < 1000$) | 適合海量數據處理 | $n$ 越大，進階演算法優勢越明顯 |
| **$n=5000$ 耗時** | 約 $0.5 \sim 1.3$ 秒 | 耗時小於 $0.01$ 秒 | 兩者效能差距達百倍以上 |
| **效能成長幅度** | 呈指數級大幅成長 | 成長極為平緩穩定 | 完美印證時間複雜度理論 |
| **實務推薦演算法** | **Insertion Sort** (基礎類最優) | **Quick Sort** (實測速度最快) | 應根據應用場景選擇適當工具 |

### 效能觀察重點：
1. **效率分水嶺**：在 $n=1000$ 之後，$O(n^2)$ 與 $O(n \log n)$ 演算法的執行時間產生質變，基礎排序法開始出現明顯體感延遲。
2. **實測效率倍率**：在最大測試規模下，Quick Sort 的排序效率比 Bubble Sort 快了約 **220 倍**，展現了優化演算法在巨量資料處理中的關鍵價值。

## 5. 心得與結論
透過本次實驗，我觀察到理論時間複雜度與實際執行效率之間有著高度的一致性。

### 個人觀察與分析：
- **實用性判斷**：雖然 Bubble Sort 邏輯最簡單，但在實際應用中效率極低，僅適合教學或數據極小時使用。
- **穩定性的考量**：在需要保持相同數值原始相對順序的場景下，Merge Sort 是比 Quick Sort 更好的選擇。
- **空間與時間的權衡**：Merge Sort 雖然快速且穩定，但其 $O(n)$ 的額外空間需求在記憶體受限的環境中（如嵌入式系統）需謹慎考慮。

**總結**：選擇排序演算法時，必須根據數據規模、記憶體限制以及是否需要穩定性來綜合評估，沒有絕對最好的演算法，只有最適合該情境的工具。